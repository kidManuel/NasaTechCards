import { createUseStyles } from 'react-jss'

const styles = createUseStyles({
    container: {
        width: '60vw',
        height: '90vh',
        display: 'flex',
        flexWrap: 'wrap',
        overflowY: 'scroll'
    }
}
)

export default styles;
